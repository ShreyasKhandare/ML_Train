services:
  - type: web
    name: ml-train
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python -m gunicorn --workers 1 --bind 0.0.0.0:$PORT app:app"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
      - key: PORT
        value: "5000"
